# 簡易介紹網頁

本專案在工作目錄下包含一個靜態介紹網頁：

- `index.html`
- `styles.css`
- `script.js`

快速在本機查看：

1. 直接在瀏覽器開啟 `index.html`。
2. 或在終端啟動簡易伺服器（例如使用 Python）：

```bash
python3 -m http.server 8000
# 然後在瀏覽器開啟 http://localhost:8000
```
